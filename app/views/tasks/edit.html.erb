    <% content_for(:title) { t(".title") } %>

    <turbo-frame id="<%= dom_id(@task) %>" class="block bg-base-200/40 px-3 py-3">
      <%= form_with model: @task do |f| %>
        <%= render "fields", f: f, task: @task %>
      <% end %>
    </turbo-frame>

    <br />

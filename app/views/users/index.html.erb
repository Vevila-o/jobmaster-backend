

  <% content_for(:title) { t(".title") } %>

      <table>
        <thead>
          <tr>
            <td> <%=User.human_attribute_name(:id)%> </td>
            <td> <%=User.human_attribute_name(:name)%> </td>
            <td> <%=User.human_attribute_name(:email)%> </td>
            <td> <%=User.human_attribute_name(:role)%> </td>
          </tr>
        </thead>

        <tbody>  
          <%= render partial: "user", collection: @users, locals: { actions: true } %>
        </tbody>
      </table>
      <br>
      
      <%= link_to t("navigation.tasks_path"), "/tasks" %>
      <%= link_to t("navigation.new_user_path"), new_user_path(@user), data: {turbo: false} %>
